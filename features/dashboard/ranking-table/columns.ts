// Single source of truth for the recommendation results grid template so
// the column header and each recommendation row share identical track widths.
export const RECOMMENDATION_GRID_COLUMNS =
  "4rem minmax(9rem, 1.5fr) 7rem minmax(8rem, 1.6fr)";

export const RECOMMENDATION_GRID_CLASS =
  "grid gap-[clamp(0.75rem,1.2vw,1rem)] px-[clamp(1rem,1.8vw,1.5rem)]";
